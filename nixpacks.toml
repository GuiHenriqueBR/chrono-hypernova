[phases.setup]
nixPkgs = ["nodejs_22", "npm-9_x"]

[phases.install]
cmds = ["cd backend && npm ci --cache /tmp/npm-cache"]

[phases.build]
cmds = ["cd backend && npm run build"]
# Disable default caching that causes EBUSY
cacheDirectories = []

[start]
cmd = "cd backend && npm start"
