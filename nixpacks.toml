# Backend deployment config (Railway uses rootDirectory = "backend")
[phases.setup]
nixPkgs = ["nodejs_22", "npm-9_x"]

[phases.install]
cmds = ["npm ci --cache /tmp/npm-cache"]

[phases.build]
cmds = ["npm run build"]
cacheDirectories = []

[start]
cmd = "npm start"
