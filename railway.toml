[build]
builder = "nixpacks"

[[services]]
name = "backend"
root = "backend"
buildCommand = "npm run build"
startCommand = "npm start"
[services.env]
PORT = "3000"

[[services]]
name = "evolution-api"
root = "services/evolution-api"
source = "Dockerfile"
[services.env]
SERVER_PORT = "8080"
