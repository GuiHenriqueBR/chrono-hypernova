[[services]]
name = "backend"
root = "backend"
builder = "nixpacks"
watchPatterns = ["backend/**"]
[services.env]
PORT = "3000"

[[services]]
name = "evolution-api"
root = "services/evolution-api"
builder = "docker"
dockerfilePath = "Dockerfile"
[services.env]
SERVER_PORT = "8080"
